name: Continuous Integration

# Simple CI workflow using Makefile commands - easy to replicate in any CI/CD system!
# For other CI systems, just use: make install && make quality

on:
  push:
    branches: [ main, develop, fix/docker-version-injection ]
  pull_request:
    branches: [ main ]

jobs:
  ci:
    name: CI Pipeline
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: '1.x'

      - name: Install dependencies
        run: make install

      - name: Run comprehensive quality checks
        run: make quality-ci

      - name: Show project information
        run: make info

  # Simplified test on Ubuntu only for now
  build-test:
    name: Build Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: '1.x'

      - name: Install and test
        run: |
          make install
          make quality-ci

      - name: Test build for current platform
        run: make build-ci
