# Jenkins MCP Server Environment Configuration
# Copy this to .env.local and configure with your Jenkins settings

# Jenkins Server URL (required)
JENKINS_URL=http://localhost:8080

# Jenkins Authentication (choose one method)
# Method 1: Username + API Token (recommended)
JENKINS_USERNAME=your-username
JENKINS_API_TOKEN=your-api-token

# Method 2: Username + Password (less secure)
# JENKINS_USERNAME=your-username  
# JENKINS_PASSWORD=your-password

# SSL/TLS Configuration (for HTTPS Jenkins servers)
# SSL Certificate Verification (default: true)
JENKINS_SSL_VERIFY=true

# Allow self-signed certificates (default: false, use with caution)
JENKINS_SSL_ALLOW_SELF_SIGNED=false

# Custom CA Certificate (for internal/organizational CA)
# JENKINS_CA_CERT_PATH=/path/to/company-ca.pem
# JENKINS_CA_CERT_CONTENT="-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----"

# Client Certificate for Mutual TLS (if required)
# JENKINS_CLIENT_CERT_PATH=/path/to/client.pem
# JENKINS_CLIENT_KEY_PATH=/path/to/client.key
# JENKINS_CLIENT_CERT_CONTENT="-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----"
# JENKINS_CLIENT_KEY_CONTENT="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"

# SSL Debugging (default: false)
JENKINS_SSL_DEBUG=false

# Optional Configuration
JENKINS_TIMEOUT=30000
JENKINS_RETRIES=3

# Logging Configuration
LOG_LEVEL=info

# Example for common Jenkins configurations:

# Local Jenkins instance
# Local Jenkins instance
# JENKINS_URL=http://localhost:8080
# JENKINS_USERNAME=admin
# JENKINS_API_TOKEN=11234567890abcdef1234567890abcdef12

# Corporate Jenkins instance with custom CA certificate
# JENKINS_URL=https://jenkins.company.com
# JENKINS_USERNAME=john.doe
# JENKINS_API_TOKEN=11234567890abcdef1234567890abcdef12
# JENKINS_CA_CERT_PATH=/etc/ssl/certs/company-ca.pem
# JENKINS_SSL_VERIFY=true

# Jenkins in Docker
# JENKINS_URL=http://localhost:8080
# JENKINS_USERNAME=admin
# JENKINS_API_TOKEN=docker-generated-token

# Jenkins with self-signed certificate (testing only)
# JENKINS_URL=https://jenkins-test.local
# JENKINS_USERNAME=test-user
# JENKINS_API_TOKEN=test-token
# JENKINS_SSL_ALLOW_SELF_SIGNED=true
# JENKINS_SSL_DEBUG=true
